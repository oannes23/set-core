# ui/

Rendering, CSS, fx, coaching, briefing. The one layer to **intentionally rebuild** behind the
clean engine boundary (TODO.md §A, step 5) — least principled, where the bugs live. Preserve the
hard-won *feel* decisions (burst format, flash/shake intensities, animation timings, coaching flow)
as spec when re-implementing.
