# macOS
.DS_Store

# Editor / OS cruft
*.swp
*~
.idea/
.vscode/

# Node / build
node_modules/
dist/
coverage/
*.tsbuildinfo

# scratch / external scaffolds
tmp/

# Local env overrides (personal, never committed). `.env` itself IS committed.
.env.local
.env.*.local
